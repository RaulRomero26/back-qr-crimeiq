# Comandos para una correcta instalacion

```
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

Crear el archivo .env en la raiz del proyecto, agregar las variables como las siguientes: 

```
MONGO_URI=
```